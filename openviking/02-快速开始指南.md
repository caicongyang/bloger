# OpenViking 快速开始指南

> 5 分钟快速上手 OpenViking

## 目录

1. [环境准备](#1-环境准备)
2. [安装 OpenViking](#2-安装-openviking)
3. [配置模型服务](#3-配置模型服务)
4. [运行第一个示例](#4-运行第一个示例)
5. [核心操作演示](#5-核心操作演示)
6. [进阶使用](#6-进阶使用)

---

## 1. 环境准备

### 1.1 系统要求

| 要求 | 最低版本 |
|------|----------|
| Python | 3.10+ |
| 操作系统 | Linux / macOS / Windows |
| 网络 | 需要访问模型服务 |

### 1.2 检查 Python 版本

```bash
python --version
# 确保输出 >= Python 3.10
```

---

## 2. 安装 OpenViking

### 2.1 使用 pip 安装

```bash
pip install openviking
```

### 2.2 验证安装

```bash
python -c "import openviking; print(openviking.__version__)"
```

---

## 3. 配置模型服务

### 3.1 创建配置文件

```bash
mkdir -p ~/.openviking
touch ~/.openviking/ov.conf
```

### 3.2 配置火山引擎（推荐）

编辑 `~/.openviking/ov.conf`：

```json
{
  "embedding": {
    "dense": {
      "api_base": "https://ark.cn-beijing.volces.com/api/v3",
      "api_key": "YOUR_VOLCENGINE_API_KEY",
      "provider": "volcengine",
      "dimension": 1024,
      "model": "doubao-embedding-vision-250615"
    }
  },
  "vlm": {
    "api_base": "https://ark.cn-beijing.volces.com/api/v3",
    "api_key": "YOUR_VOLCENGINE_API_KEY",
    "provider": "volcengine",
    "model": "doubao-seed-1-8-251228"
  }
}
```

### 3.3 配置 OpenAI（备选）

```json
{
  "embedding": {
    "dense": {
      "api_base": "https://api.openai.com/v1",
      "api_key": "YOUR_OPENAI_API_KEY",
      "provider": "openai",
      "dimension": 3072,
      "model": "text-embedding-3-large"
    }
  },
  "vlm": {
    "api_base": "https://api.openai.com/v1",
    "api_key": "YOUR_OPENAI_API_KEY",
    "provider": "openai",
    "model": "gpt-4-vision-preview"
  }
}
```

> 📝 **提示**：火山引擎新用户有免费额度，性价比高

---

## 4. 运行第一个示例

### 4.1 创建示例脚本

创建 `example.py`：

```python
import openviking as ov

# 1. 初始化客户端
client = ov.OpenViking(path="./data")

try:
    # 2. 初始化
    client.initialize()
    print("✅ OpenViking 初始化完成")

    # 3. 添加资源（支持 URL、文件、目录）
    print("📥 添加资源...")
    add_result = client.add_resource(
        path="https://raw.githubusercontent.com/volcengine/OpenViking/refs/heads/main/README.md"
    )
    root_uri = add_result['root_uri']
    print(f"   资源 URI: {root_uri}")

    # 4. 浏览目录结构
    print("\n📁 目录结构:")
    ls_result = client.ls(root_uri)
    print(f"   {ls_result}")

    # 5. 使用 glob 查找文件
    print("\n🔍 查找 Markdown 文件:")
    glob_result = client.glob(pattern="**/*.md", uri=root_uri)
    if glob_result['matches']:
        content = client.read(glob_result['matches'][0])
        print(f"   找到 {len(glob_result['matches'])} 个文件")
        print(f"   内容预览: {content[:200]}...")

    # 6. 等待语义处理完成
    print("\n⏳ 等待语义处理...")
    client.wait_processed()

    # 7. 获取摘要和概览
    print("\n📄 获取摘要和概览:")
    abstract = client.abstract(root_uri)
    overview = client.overview(root_uri)
    print(f"   摘要 (L0): {abstract[:100]}...")
    print(f"   概览 (L1): {overview[:200]}...")

    # 8. 语义搜索
    print("\n🔎 执行语义搜索:")
    results = client.find("what is openviking", target_uri=root_uri)
    print(f"   找到 {len(results.resources)} 个结果:")
    for r in results.resources:
        print(f"   - {r.uri} (score: {r.score:.4f})")

    print("\n🎉 示例运行完成!")

except Exception as e:
    print(f"❌ 错误: {e}")
    import traceback
    traceback.print_exc()

finally:
    # 9. 关闭客户端
    client.close()
    print("👋 客户端已关闭")
```

### 4.2 运行脚本

```bash
python example.py
```

### 4.3 预期输出

```
✅ OpenViking 初始化完成
📥 添加资源...
   资源 URI: viking://resources/README.md

📁 目录结构:
   ...

🔍 查找 Markdown 文件:
   找到 1 个文件
   内容预览: ...

⏳ 等待语义处理...
📄 获取摘要和概览:
   摘要 (L0): OpenViking: 为 AI Agent 而生的上下文数据库...
   概览 (L1): # OpenViking...

🔎 执行语义搜索:
   找到 3 个结果:
   - viking://resources/README.md (score: 0.8523)
   - viking://resources/README.md#architecture (score: 0.7891)
   - viking://resources/README.md#features (score: 0.7654)

🎉 示例运行完成!
👋 客户端已关闭
```

---

## 5. 核心操作演示

### 5.1 文件系统操作

```python
import openviking as ov

client = ov.OpenViking(path="./data")
client.initialize()

# 列出目录
result = client.ls("viking://resources/")
print(result)

# 创建目录
client.mkdir("viking://resources/myproject/docs")

# 读取文件
content = client.read("viking://resources/README.md")

# 获取摘要 (L0)
abstract = client.abstract("viking://resources/README.md")

# 获取概览 (L1)
overview = client.overview("viking://resources/README.md")

# 移动/重命名
client.mv("viking://resources/old", "viking://resources/new")

# 删除
client.rm("viking://resources/temp")

client.close()
```

### 5.2 资源管理

```python
# 添加本地文件
result = client.add_resource(path="./docs")
print(result['root_uri'])

# 添加 URL
result = client.add_resource(
    path="https://example.com/docs.pdf"
)
print(result['root_uri'])

# 添加 GitHub 仓库
result = client.add_resource(
    path="https://github.com/user/repo"
)
print(result['root_uri'])

# 等待处理完成
client.wait_processed()
```

### 5.3 语义搜索

```python
# 简单搜索 (find)
results = client.find(
    query="OAuth 认证方法",
    target_uri="viking://resources/docs"
)

for r in results.resources:
    print(f"{r.uri} (score: {r.score:.4f})")

# 复杂搜索 (search) - 需要会话
session = client.session()
results = await client.search(
    query="帮我创建 RFC 文档",
    session_info=session
)
```

### 5.4 会话管理

```python
# 创建会话
session = client.session(session_id="chat_001")

# 添加用户消息
session.add_message(
    "user",
    [ov.TextPart("如何配置 embedding?")]
)

# 添加助手消息
session.add_message(
    "assistant",
    [
        ov.TextPart("配置方法如下..."),
        ov.ContextPart(uri="viking://resources/config.md")
    ]
)

# 记录使用的上下文
session.used(contexts=["viking://user/memories/profile.md"])

# 提交会话（归档 + 提取记忆）
result = session.commit()
print(result)
```

---

## 6. 进阶使用

### 6.1 HTTP 服务模式

**服务端启动**:

```bash
# 启动 HTTP 服务
python -m openviking serve --port 1933
```

**客户端连接**:

```python
import openviking as ov

# 连接到远程服务
client = ov.SyncHTTPClient(
    url="http://localhost:1933",
    api_key="your-api-key"
)

# 使用方式相同
result = client.find("query", target_uri="viking://resources/")
```

### 6.2 异步使用

```python
import asyncio
import openviking as ov

async def main():
    client = ov.OpenViking(path="./data")
    await client.initialize()
    
    # 异步添加资源
    result = await client.add_resource(path="./docs")
    
    # 异步搜索
    results = await client.find("query")
    
    await client.close()

asyncio.run(main())
```

### 6.3 自定义 Rerank

```python
# 在配置中添加 rerank
config = {
    "rerank": {
        "api_base": "https://ark.cn-beijing.volces.com/api/v3",
        "api_key": "your-api-key",
        "provider": "volcengine",
        "model": "doubao-seed-rerank"
    }
}

# 使用 THINKING 模式自动启用 Rerank
results = await client.search(
    "复杂查询",
    mode=ov.SearchMode.THINKING
)
```

---

## 下一步

- 📖 阅读 [架构详解](./01-项目概览与架构.md) 深入理解系统设计
- 🔧 查看 [API 参考](./api-reference.md) 了解更多接口
- 🚀 探索 [部署指南](./deployment.md) 生产环境部署
